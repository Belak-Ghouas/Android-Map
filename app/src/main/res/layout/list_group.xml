<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">


    <TextView
        android:id="@+id/listTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_xl_2"
        android:paddingBottom="@dimen/margin_normal"
        android:paddingTop="@dimen/margin_normal"
        android:layout_marginLeft="@dimen/margin_normal_20dp"
        android:layout_marginRight="@dimen/margin_normal_20dp"
        android:paddingLeft="@dimen/margin_normal_20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:textColor="@color/white"
        android:background="@drawable/rounded_color_bluedark"
        tools:text="Usine de Poissy"
        />

    <ImageView
        android:id="@+id/iv_section_action_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_section_expand"
        app:layout_constraintBottom_toBottomOf="@id/listTitle"
        app:layout_constraintTop_toTopOf="@id/listTitle"
        app:layout_constraintEnd_toEndOf="@id/listTitle"/>

</androidx.constraintlayout.widget.ConstraintLayout>